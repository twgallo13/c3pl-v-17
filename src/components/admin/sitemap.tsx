// V17.1.2-p5b — Admin Sitemap (compact JSX to avoid truncation)
import React from 'react';
import { ROUTES } from '@/routes/registry';

      <div className="overflow-x-auto">
          
              <th className="py-2 pr-3">
              <th className="py-2 pr-3">Workflow</t
              <th className="py-2 pr-3"
            </tr>
          <tbody>
              <tr key={r.path} className="borde
                <td className="py-2 pr-3">{r.path}
                <td className="py-2 pr-3">{r.role
                <td className="py-2">{r.version ?? '—
            ))}
        </table>
    </div>
}

















