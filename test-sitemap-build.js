// V17.1.2-p5a — Test sitemap build and routing


// Check all necessary files exist
const fs = require('fs');
const path = require('path');

  'src/lib/rbac

files.forEach(file => {
  console.log(`${exists ? '✅' : '❌'} ${

const appContent = fs.r
console.log(`${usesCorrect
// Check sitemap ro
co

// Check version is set to p5a
console.log(`${versionS
console.log('\n✅ V17.1.2-p5a implementation complete:');
console.log('- Footer crumb displays Workflow •
con





















